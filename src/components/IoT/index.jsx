import './styles.scss';

const IoT = ({ iotRef }) => {
  return (
    <section ref={iotRef} className="iot">
      <div className="iot__container container"></div>
    </section>
  );
};

export default IoT;
